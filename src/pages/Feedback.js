import React from 'react';
import Header from '../components/Header';
// import { connect } from 'react-redux';

const Feedback = () => (
  <div>
    <Header />
    <h1>Página Feedback</h1>
  </div>
);


export default Feedback;
